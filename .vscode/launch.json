{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (GDB)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/a.out", // Path to your compiled executable
            "args": [], // Command-line arguments for your program
            "stopAtEntry": false, // Whether to stop at the program's entry point
            "cwd": "${workspaceFolder}", // Working directory for the debugger
            "environment": [], // Environment variables
            "externalConsole": false, // Use an external console for program output
            "MIMode": "gdb", // Specify GDB as the debugger
            "miDebuggerPath": "/usr/bin/gdb", // Path to your GDB executable (adjust for Windows/macOS)
            "setupCommands": [
                {
                    "description": "Enable pretty printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
